# Gemini_proxy_claudecode - Environment Variables
# Copy this file to .env and fill in your values

# OAuth is used by default - NO API KEY NEEDED!
# Just run `start-gemini-proxy` and login via browser

# Optional: Server port (default: 8081)
PORT=8081

# Optional: Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Optional: Custom OAuth client (not needed for most users)
# GOOGLE_CLIENT_ID=your-client-id
# GOOGLE_CLIENT_SECRET=your-client-secret
